#include "changePoint.h"
#include <R_ext/Rdynload.h>

/*
void R_init_rterm(DllInfo *info) {
  R_RegisterCCallable(info, "findChangePoint", (DL_FUNC) &findChangePoint);
  R_RegisterCCallable(info, "cpl1", (DL_FUNC) &cpl1);
  R_RegisterCCallable(info, "bootstrapChangePoint", (DL_FUNC) &bootstrapChangePoint);
}
*/